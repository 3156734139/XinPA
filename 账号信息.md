# 陪玩星助手 - 账号与配置信息

---

## 一、本地服务

### 1. MySQL 数据库
| 项目 | 值 |
|------|-----|
| 地址 | localhost:3307 |
| 用户名 | root |
| 密码 | root |
| 数据库名 | xinpa |

### 2. Redis 缓存
| 项目 | 值 |
|------|-----|
| 地址 | localhost:6379 |
| 密码 | 无（免密认证） |
| 数据库编号 | 0 |

### 3. RabbitMQ 消息队列
| 项目 | 值 |
|------|-----|
| 连接地址 | localhost:5672 |
| 管理后台 | http://localhost:15672 |
| 用户名 | guest |
| 密码 | guest |
| 虚拟主机 | / |

### 4. MinIO 文件存储（已配置但未启动）
| 项目 | 值 |
|------|-----|
| 控制台地址 | http://localhost:9000 |
| Access Key | minioadmin |
| Secret Key | minioadmin |
| 存储桶 | xinpa |
| 状态 | ❌ 未启动，文件上传功能暂不可用 |

---

## 二、应用访问

| 项目 | 地址 |
|------|------|
| 前端页面 | http://localhost:3000 |
| 后端 API | http://localhost:8080/api |

---

## 三、测试账号

### 普通用户
| 项目 | 值 |
|------|-----|
| 用户名 | test001 |
| 密码 | 123456 |
| 登录地址 | http://localhost:3000 |
| 角色 | 陪玩师（可管理订单、客户、财务、AI 工具等） |

### 管理员
| 项目 | 值 |
|------|-----|
| 用户名 | admin |
| 密码 | admin123 |
| 登录地址 | http://localhost:3000 → 左下角「管理员登录」 |
| 角色 | 系统管理员（可查看统计、管理公告、AI 日志等） |

---

## 四、第三方 API（需自行配置）

### AI 大模型接口（application.yml 中配置）
| 项目 | 值 |
|------|-----|
| 接口地址 | https://api.openai.com/v1/chat/completions |
| API Key | **your-api-key-here**（需替换为你自己的 Key） |
| 模型 | gpt-3.5-turbo |
| 免费用户每日限制 | 10 次 |
| VIP 用户每日限制 | 9999 次 |

> 如需使用 AI 功能（生成介绍、开场白、安慰文案、话术、活动文案、风险检测），请将 `application.yml` 中的 `ai.api-key` 替换为有效的 API Key。

---

## 五、服务启停命令

```bash
# MySQL（Homebrew 安装，端口 3307）
brew services start mysql@8.4
brew services stop mysql@8.4

# Redis
brew services start redis
brew services stop redis

# RabbitMQ
brew services start rabbitmq
brew services stop rabbitmq

# MinIO（如需启动）
minio server /path/to/data --console-address :9001

# 后端（项目目录下）
cd backend
export JAVA_HOME=/opt/homebrew/opt/openjdk@17
mvn spring-boot:run

# 前端（项目目录下）
cd frontend
npm run dev
```
